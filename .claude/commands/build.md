프로젝트를 빌드하고 에러가 있으면 수정해줘.

## Instructions

1. package.json에서 build 스크립트 확인
2. 빌드 실행
3. 에러 발생 시:
   - 타입 에러 → 해당 파일 확인 후 수정
   - 빌드 설정 에러 → 설정 또는 import 문제 확인 후 수정
   - 수정 후 다시 빌드하여 성공 확인
4. 빌드 성공 시 결과 요약 (번들 크기, 파일 수 등)
